require 'spec_helper'

describe 'UsuarioCreator' do

  # it 'deber crear y guardar un usuario' do
  #   nombre = 'Juan'
  #   direccion = 'La Pampa 123'
  #   telefono = 12345678
  #   usuario = instance_double(Usuario)
  #   repo = instance_double(Persistencia::Repositorios::RepositorioUsuario)
  #   expect(Usuario).to receive(:new).with(nombre, direccion, telefono, nil).and_return(usuario)
  #   expect(repo).to receive(:guardar).with(usuario).and_return(usuario)
  #   UsuarioCreator.new(repo).crear_usuario('Juan', 'La Pampa 123', 12345678, nil)
  # end

end
